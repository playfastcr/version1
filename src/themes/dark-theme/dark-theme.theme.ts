import { createTheme } from '@mui/material';

import { palette, typography } from './dark-theme.constants';

/* ––
 * –––– Theme definition
 * –––––––––––––––––––––––––––––––––– */
export const darkTheme = createTheme({
  palette,
  typography,
});
